moduledir = pkgdatadir / meson.project_name() / 'widgets' / 'quicksettings'

modules_sources = [
    '__init__.py',
    'bluetooth.py',
    'button.py',
    'network.py',
    'power.py',
    'menu.py',
]

install_data(modules_sources, install_dir: moduledir)