moduledir = pkgdatadir / meson.project_name() / 'widgets' / 'bar'

modules_sources = [
    '__init__.py',
    'tray.py',
    'music.py',
    'audio.py'
]

install_data(modules_sources, install_dir: moduledir)